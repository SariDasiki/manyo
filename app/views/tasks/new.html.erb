<h1><%= t('tasks.new') %></h1>

<%= form_with(model: @task, local: true) do |f| %>
  <div>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit "create task " %>
  </div>
<%= link_to t('戻る'), tasks_path%>
<% end %>