<h1><%= t('tasks.show') %></h1>
<p>タイトル: <%= @task.title %></p>
<p>内容: <%= @task.content %></p>
<p>作成日時: <%= @task.created_at %></p>
<p>終了期限: <%= @task.deadline_on %></p>
<p>優先度: <%= @task.priority %></p>
<p>ステータス: <%= @task.status %></p>

<div class="field">
  ラベル: <% @task.labels.each do |label| %>
  <%= label.name %>
  <% end %>
  </div>
</div>
<br>
<div class="field">
  <%= link_to t('common.edit'), edit_task_path(@task), id: "edit-task"%>
  <%= link_to t('common.back'), tasks_path, id: "back"%>
</div>